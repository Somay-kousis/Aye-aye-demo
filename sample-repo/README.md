# northwind

Account, billing and notification services.

- `services/auth` — sessions, refresh rotation, token signing
- `services/payments` — charges, refunds, gateway webhooks
- `services/admin` — the internal console
- `services/notifications` — email templates and dispatch
- `infra/cache` — process-local caches
